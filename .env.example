# Mythoria Admin Portal Environment Variables

# Database URLs (to be configured in Phase 3)
# DATABASE_URL=postgresql://username:password@localhost:5432/mythoria_db
# WORKFLOWS_DATABASE_URL=postgresql://username:password@localhost:5432/workflows_db
# BACKOFFICE_DATABASE_URL=postgresql://username:password@localhost:5432/backoffice_db

# Authentication (to be configured in Phase 4)
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=your-secret-here
# MAGIC_LINK_SECRET=your-magic-link-secret

# Email service (to be configured in Phase 4)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Cloud services (to be configured in Phase 2)
# GOOGLE_CLOUD_PROJECT_ID=oceanic-beach-460916-n5
# GOOGLE_CLOUD_REGION=europe-west9
